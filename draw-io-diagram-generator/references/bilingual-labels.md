# Language and bilingual labels

Choose Chinese, English, or `中文 / English` before sizing and positioning. Apply the chosen convention consistently to titles, lanes, ports, adapters, and edge labels. Estimate line count before layout; shorten labels rather than shrinking nodes below their readable minimum. Use `身份端口 / Identity Port` style labels, not mixed untranslated peers.
