# Regression fixtures

These intentionally-invalid fixtures exercise geometry checks. Run `validate-layout.py` against the first three; each must fail. `misleading-adapter-relation.drawio` is a semantic review fixture: it shows the prohibited adapter-to-adapter direction. `bilingual-overflow.drawio` must fail the bilingual width rule.
